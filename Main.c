#pragma config(Motor,  port2,           rightMotor, tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port3,           leftMotor, tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port4,           intakeLeft,    tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port5,           intakeRight,   tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port6,           outtakeLeft,   tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port7,           outtakeRight,  tmotorServoContinuousRotation, openLoop)
#pragma DebuggerWindows("debugStream")
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

//+++++++++++++++++++++++++++++++++++++++++++++| MAIN |+++++++++++++++++++++++++++++++++++++++++++++++


const int fastSensitivity = 2;
//Main Driving Loop
task driveLoop(){
	while(1 == 1)
	  {
	    motor[rightMotor] = vexRT[Ch2];
	    motor[leftMotor] = vexRT[Ch3];
	    writeDebugStream("RIGHT: %d\nLEFT: %d\n",vexRT[Ch2],vexRT[Ch3]);
		
	    EndTimeSlice();
	  }
}

//Drive Fast
task fastDriveLoop(){
	while(1==1){


		motor[rightMotor] = (255- (vexRT[Ch1]/fastSensitivity));
		motor[leftMotor] = (255 + (vexRT[Ch1]/fastSensitivity));
		writeDebugStream("RIGHT: %d\nLEFT: %d\n",(255-vexRT[Ch1]/fastSensitivity),(255+vexRT[Ch1]/fastSensitivity));
		EndTimeSlice();'
	}
}

task main()
{
  startTask(driveLoop);
  while(1==1){

  //Accelerate
	 	if(vexRT[Btn6D] == 1){
	 		stopTask(driveLoop);
	 		startTask(fastDriveLoop);
	 	}
	 	else{
	 		stopTask(fastDriveLoop);
	 		startTask(driveLoop);
	 	}


	 	//Intake Motors
 	 if(vexRT[Btn5U] == 1){
 		motor[intakeLeft] = 255;
 		motor[intakeRight] = 255;
	 	}
	 	else{
	 		motor[intakeLeft] = 0;
	 		motor[intakeRight] = 0;
	 	}


	 	//Outtake Motors
	 	if(vexRT[Btn5D] == 1){
 		motor[outtakeLeft] = 255;
 		motor[outtakeRight] = 255;
	 	}
	 	else{
	 		motor[outtakeLeft] = 0;
	 		motor[outtakeRight] = 0;
	 	}


	 	EndTimeSlice();
	}

}




//++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
